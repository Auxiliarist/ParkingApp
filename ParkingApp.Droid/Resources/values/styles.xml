<?xml version="1.0" encoding="utf-8" ?>
<resources>

  <style name="MyTheme" parent="MyTheme.Base">
    <item name="windowNoTitle">true</item>
    <!--We will be using the toolbar so no need to show ActionBar-->
    <item name="windowActionBar">false</item>
  </style>

  <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
  -->

  <style name="MyTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- colorPrimary is used for the default action bar background -->
    <item name="colorPrimary">@color/primary</item>
    <!-- colorPrimaryDark is used for the status bar -->
    <item name="colorPrimaryDark">@color/primaryDark</item>
    <!-- colorAccent is used as the default value for colorControlActivated which is used to tint widgets -->
    <item name="colorAccent">@color/accent</item>
    <!-- You can also set colorControlNormal, colorControlActivated
         colorControlHighlight and colorSwitchThumbNormal. -->
  </style>

  <!--
  
  <style name="MyTheme.Settings" parent="MyTheme">
    <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
  </style>
  
  -->

  <style name="MyTheme.Settings" parent="MyTheme">
    <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
  </style>


  <!--

  <style name="MyTheme.Splash" parent="Theme.AppCompat.Light.NoActionBar">
    <item name="android:windowBackground">@drawable/splash</item>
  </style>
  
  -->

  <!--
  
  <style name="MyTheme.BottomNavigation">
    <item name="android:background">@android:color/white</item>
    <item name="itemBackground">@drawable/main_navigation_bg</item>
    <item name="itemIconTint">@color/main_navigation_color</item>
    <item name="itemTextColor">@color/main_navigation_color</item>
    <item name="elevation">8dp</item>
  </style>

  <style name="MyTheme.FAB.Spot"
         parent="Theme.AppCompat.Light">
    <item name="colorAccent">@color/primary</item>
  </style>

  <style name="MyTheme.FAB.Meter"
         parent="Theme.AppCompat.Light">
    <item name="colorAccent">@color/accent</item>
  </style>
  
  -->

</resources>